import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    baseText: {
        fontFamily: 'Kalam'
    },
    container: {
        padding: 15,
        width: '100%',
        display: 'flex',
    },
    title: {
        fontFamily: 'Jua',
        fontSize: 20,
        fontWeight: '400',
        marginBottom: 15,
        textAlign: 'center',
    },
});

export default styles;